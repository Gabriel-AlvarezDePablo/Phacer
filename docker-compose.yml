services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: phacer
      POSTGRES_PASSWORD: phacer_dev
      POSTGRES_DB: phacer
    ports:
      - "5432:5432"
    volumes:
      - phacer_pgdata:/var/lib/postgresql/data

volumes:
  phacer_pgdata:
